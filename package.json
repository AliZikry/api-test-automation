{
  "name": "api-test-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "docker-build": "docker build -t cypress-app .",
    "docker-test": "docker run -it --rm cypress-app",
    "docker-up": "docker-compose up --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.15.0"
  }
}
